# Test and lint tools. Install the locked runtime first:
#   pip install --require-hashes -r requirements.txt && pip install -r requirements-dev.txt
pytest>=9.1.1
ruff>=0.5
aiosmtpd>=1.4
pip-audit>=2.7
